import React from 'react';
import ReactDOM from 'react-dom/client';
import AuditTool from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AuditTool />
  </React.StrictMode>
);
